node_modules
.svelte-kit
/frontend/build
.DS_Store
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# .NET
**/bin/
**/obj/
*.user
.vs/

# Caddy origin cert / private key — referenced by docker-compose.prod.yml,
# placed on the prod host only. NEVER commit (Cloudflare Origin CA private key).
/caddy/
